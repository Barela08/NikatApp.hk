==============================
NIKAT BACKEND ARCHITECTURE
==============================

Backend Type:
- Firebase Backend
- Firestore Database
- Firebase Authentication
- Firebase Storage
- Firebase Cloud Messaging
- REST API Ready Structure

========================================
1. FIREBASE COLLECTION STRUCTURE
========================================

----------------------------------------
users/
----------------------------------------

Document ID:
uid

Fields:
{
  uid: string,
  name: string,
  email: string,
  phone: string,
  profileImage: string,
  loginType: string,
  language: string,
  theme: string,
  isOnline: boolean,
  isVerified: boolean,
  createdAt: timestamp,
  updatedAt: timestamp,
  lastLogin: timestamp,
  fcmToken: string
}

----------------------------------------
user_settings/
----------------------------------------

Document ID:
uid

Fields:
{
  notificationsEnabled: boolean,
  darkMode: boolean,
  selectedLanguage: string,
  autoSync: boolean,
  cacheEnabled: boolean,
  updatedAt: timestamp
}

----------------------------------------
notifications/
----------------------------------------

Document ID:
autoGenerated

Fields:
{
  notificationId: string,
  userId: string,
  title: string,
  body: string,
  image: string,
  type: string,
  isRead: boolean,
  createdAt: timestamp
}

----------------------------------------
activity_logs/
----------------------------------------

Document ID:
autoGenerated

Fields:
{
  activityId: string,
  userId: string,
  action: string,
  deviceInfo: string,
  ipAddress: string,
  createdAt: timestamp
}

----------------------------------------
app_config/
----------------------------------------

Document ID:
main

Fields:
{
  maintenanceMode: boolean,
  latestVersion: string,
  forceUpdate: boolean,
  supportEmail: string,
  privacyPolicyUrl: string,
  termsUrl: string
}

----------------------------------------
feedback/
----------------------------------------

Document ID:
autoGenerated

Fields:
{
  feedbackId: string,
  userId: string,
  message: string,
  rating: number,
  createdAt: timestamp
}

========================================
2. FIREBASE AUTHENTICATION
========================================

Enable:
- Google Authentication
- Email/Password Authentication

Authentication Flow:
1. User Login
2. Firebase Auth
3. Generate UID
4. Save User in Firestore
5. Store Session
6. Redirect Dashboard

========================================
3. FIREBASE STORAGE STRUCTURE
========================================

storage/
 ├── profile_images/
 ├── app_banners/
 ├── user_uploads/
 └── temp/

========================================
4. FIREBASE SECURITY RULES
========================================

Firestore Rules:

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      allow read, write: if request.auth != null
      && request.auth.uid == userId;
    }

    match /user_settings/{userId} {
      allow read, write: if request.auth != null
      && request.auth.uid == userId;
    }

    match /notifications/{docId} {
      allow read: if request.auth != null;
      allow write: if false;
    }

    match /feedback/{docId} {
      allow create: if request.auth != null;
      allow read: if false;
    }

    match /app_config/{docId} {
      allow read: if true;
      allow write: if false;
    }
  }
}

========================================
5. FIREBASE STORAGE RULES
========================================

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    match /profile_images/{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }

    match /user_uploads/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

========================================
6. API STRUCTURE (OPTIONAL NODE.JS)
========================================

Backend Framework:
- Node.js
- Express.js

Folder Structure:

backend/
 ├── config/
 ├── controllers/
 ├── middleware/
 ├── models/
 ├── routes/
 ├── services/
 ├── utils/
 ├── firebase/
 └── server.js

========================================
7. REST API ENDPOINTS
========================================

AUTH APIs:
POST   /api/auth/google-login
POST   /api/auth/email-login
POST   /api/auth/register
POST   /api/auth/logout

USER APIs:
GET    /api/user/profile
PUT    /api/user/update
POST   /api/user/upload-image

SETTINGS APIs:
GET    /api/settings
PUT    /api/settings/update

NOTIFICATION APIs:
GET    /api/notifications
PUT    /api/notifications/read

FEEDBACK APIs:
POST   /api/feedback/send

========================================
8. RESPONSE FORMAT
========================================

Success Response:
{
  success: true,
  message: "Data fetched successfully",
  data: {}
}

Error Response:
{
  success: false,
  message: "Something went wrong",
  error: {}
}

========================================
9. NODE.JS SERVER CONFIG
========================================

Required Packages:
- express
- firebase-admin
- cors
- dotenv
- jsonwebtoken
- bcryptjs
- multer

========================================
10. ENVIRONMENT VARIABLES
========================================

.env

PORT=5000

FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

JWT_SECRET=

========================================
11. PUSH NOTIFICATION FLOW
========================================

1. User Login
2. Save FCM Token
3. Admin sends notification
4. Firebase Cloud Messaging triggers
5. User receives notification

========================================
12. APP STATE FLOW
========================================

App Open
↓
Check Auth
↓
Fetch User Data
↓
Load Settings
↓
Dashboard
↓
Real-time Updates

========================================
13. OFFLINE SUPPORT
========================================

Use:
- Hive Database
- Firestore Offline Persistence
- SharedPreferences Cache

========================================
14. ADMIN READY STRUCTURE
========================================

Future Admin Features:
- User Management
- Analytics Dashboard
- Notification Panel
- Content Control
- Ban/Unban Users
- App Monitoring

========================================
15. FINAL BACKEND GOAL
========================================

Build a scalable, secure, production-ready backend for the NIKAT Flutter application using Firebase + REST APIs with:

- Google Login
- Firestore Database
- Secure Authentication
- Notification System
- Offline Support
- User Management
- Scalable Architecture
- Production Security
- High Performance